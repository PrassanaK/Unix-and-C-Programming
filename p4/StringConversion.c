#include <stdio>
#include <string.h>
#include <stdlib.h>

void convertString(char* charArray[], int inArray[], int length)
{
    int val;
    charArray;

    int ii;
    for(ii=0; ii<length; ii++)
    {
        val = atoi(str[ii]);
        intArray[ii] = val;
    }
}
